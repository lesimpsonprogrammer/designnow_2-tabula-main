# Teams Organization Builder — Status

Feature code is implemented on `feature/organization-builder` and isolated to active Teams licenses.

Current gate: identify and connect the Supabase project used by Tabula Design Now before applying the Teams migration and merging to `main`.
