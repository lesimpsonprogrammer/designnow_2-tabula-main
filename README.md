# Tabula confirmed project backup

This is the project-tabula (11).tabula file confirmed by Larry on September 6, 2026. It contains 16 pages and embedded project assets.

Import this file using Tabula’s project import feature. This is project data, not a standalone HTML application.

Verify the original backup with `shasum -a 256 -c SHA256SUMS`. This repository preserves the confirmed file; browser edits are not automatically synchronized here.
